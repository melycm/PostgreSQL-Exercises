-- SELECT * FROM restaurant ORDER BY distance_miles_by_headquarters;

-- SELECT * FROM restaurant ORDER BY distance_miles_by_headquarters LIMIT 2;

-- SELECT * FROM restaurant ORDER BY stars LIMIT 2;

-- SELECT * FROM restaurant ORDER BY stars, distance_miles_by_headquarters <= 2 LIMIT 2;

-- SELECT SUM(stars) FROM restaurant;

-- SELECT COUNT(category_tyoe_of_food) FROM restaurant;

-- SELECT AVG(STARS) FROM restaurant;

-- SELECT MAX(stars) FROM restaurant;